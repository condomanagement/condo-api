version: '3'
services:
  web:
    image: ruby/3.2.2
    build: .
    environment:
      - RAILS_ENV=${RAILS_ENV}
    command: bundle exec rails s -p 3000 -b '0.0.0.0' -e ${RAILS_ENV}
    volumes:
      - .:/myapp
    ports:
      - "3000:3000"
    # tty: true
    # stdin_open: true
